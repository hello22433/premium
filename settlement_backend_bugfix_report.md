# 정산관리 검색 및 날짜 필터 기술 수정 보고서

본 보고서는 정산관리 시스템의 안정성과 데이터 무결성을 확보하기 위해 백엔드 레이어에서 수행된 주요 기술적 수정 사항을 정리한 것입니다.

---

## 🛠️ 공통 인프라: `QueryBuilderDateCondition` 핵심 로직 수정

기존 유틸리티의 설계 결함(9시간 시각 오차 및 파라미터 덮어쓰기)을 해결하여 모든 날짜 기반 조회 쿼리의 데이터 신뢰성을 확보했습니다.

### 1) 시점 정규화 (KST Timezone Normalization)
- **수정 기술**: `new Date()` 파싱 대신 `date-fns` 라이브러리를 도입하여 입력값을 로컬 시계 기준의 `startOfDay`, `endOfDay`로 정밀하게 정규화.
- **바인딩 방식**: 오차가 발생하기 쉬운 `Date` 객체 대신 명시적인 DB 포맷 문자열(`yyyy-MM-dd HH:mm:ss`)로 직접 바인딩하여 타임존 오차를 원천 차단함.

### 2) 동적 파라미터 키(Dynamic Parameter Key) 도입
- **수정 전**: `:startAt`, `:endAt`으로 파라미터명이 하드코딩되어 있어, 한 쿼리 내에서 복수의 날짜 컬럼(등록일, 증빙일 등)에 이 유틸리티를 중복 적용할 경우 나중에 들어온 값으로 덮어씌워지는(Shadowing) 버그 발생.
- **수정 후**: `${columnName}StartAt` 형태로 파라미터 키를 동적 생성하여 쿼리 내 고유성을 보장. 이로써 등록일자와 증빙일자에 대한 멀티 날짜 필터링이 가능함을 보장함.

---

## 🛠️ 서비스 레이어: `SettleService` 필터 간섭 로직 수정

정산 데이터 조회 시 특정 필터가 다른 필터의 조회 범위를 제한하던 로직을 디커플링했습니다.

### 1) 이중 필터링(Dual Filter) 종속성 해소
- **문제 지점**: 증빙일자(`proveAt`) 검색 시에도 서비스 레이어에서 강제로 설정된 등록일자(`createdAt`, 최근 1개월 등) 필터가 `AND`로 상시 결합되어 결과가 누락되는 구조.
- **해결 방안**: 증빙일 관련 쿼리 파라미터(`proveStartAt`, `proveEndAt`)가 유효하게 전달될 경우, 등록일자에 대한 기본 검색 조건 적용을 명시적으로 스킵(Skip)하도록 로직 보강.

---

## 🛠️ 데이터 레이어: Entity Join 및 쿼리 최적화 수정

엔티티 관계 참조 에러를 해결하고 검색 로직의 무결성을 확보했습니다.

### 1) 비즈니스 엔티티 관계 보강 (`Unknown column` 해결)
- **수정 내역**: `SettleService` 및 `OrderRealProductService`에서 `businessUser`를 통한 `businessName` 조회 시 상속 구조상 필수적인 `UserCompanyEntity`(`businessCompany`) 조인이 누락된 모든 쿼리문에 `leftJoinAndSelect` 추가.

### 2) `Brackets` 기반 글로벌 통합 검색 구현
- **기능 범위**: 고객사, 담당자, 이벤트, 상품명 등 복수 필드에 대해 `searchKeyword` 한 개로 통합 검색 지원.
- **구축**: TypeORM의 `Brackets`를 활용하여 `OR` 연산 그룹을 타 고정 필터들과의 `AND` 연산 계층에서 안전하게 격리하여 쿼리 우선순위 및 결과 정확도 확보.
